apiVersion: v2

# Name of the Helm chart
name: app-chart

# Description of what this chart deploys
description: A Helm chart for deploying the stephen-app to EKS

# Chart type — application means it deploys actual workloads
type: application

# Version of the chart itself
version: 0.1.0

# Version of the application being deployed
appVersion: "1.0.0"
